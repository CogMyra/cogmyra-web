          {/* Three persona cards */}
          <div className="mt-10 grid gap-6 md:grid-cols-3">
            {/* Students */}
            <div className="rounded-3xl border border-slate-200 bg-white/90 px-5 py-6 shadow-sm">
              <h3 className="text-xs font-semibold uppercase tracking-[0.16em] text-[#26413C]">
                For Students
              </h3>
              <p className="mt-3 text-sm leading-relaxed text-slate-800">
                Build confidence, close gaps, and prepare for exams with a coach
                that remembers what you&apos;ve already mastered and adapts to
                your pace.
              </p>
            </div>

            {/* Teachers */}
            <div className="rounded-3xl border border-slate-200 bg-white/90 px-5 py-6 shadow-sm">
              <h3 className="text-xs font-semibold uppercase tracking-[0.16em] text-[#26413C]">
                For Teachers
              </h3>
              <p className="mt-3 text-sm leading-relaxed text-slate-800">
                Design lessons, scaffolds, and checks for understanding in
                minutes—without losing your identity or expertise as an
                educator.
              </p>
            </div>

            {/* Professionals */}
            <div className="rounded-3xl border border-slate-200 bg-white/90 px-5 py-6 shadow-sm">
              <h3 className="text-xs font-semibold uppercase tracking-[0.16em] text-[#26413C]">
                For Professionals
              </h3>
              <p className="mt-3 text-sm leading-relaxed text-slate-800">
                Learn new tools, frameworks, and skills through examples,
                role-plays, and practice that matches the real work you do.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Footer – on light background so adjust text colors */}
      <footer className="border-t border-slate-300 bg-[#E3ECE8]">
        <div className="mx-auto flex max-w-5xl flex-col items-start justify-between gap-4 px-4 py-6 text-sm text-slate-600 sm:flex-row sm:items-center sm:px-6 lg:px-8">
          <p>© {year} CogMyra. All rights reserved.</p>
          <div className="flex gap-4">
            <a href="/guide" className="hover:text-slate-900">
              Guide
            </a>
            <a href="/privacy.html" className="hover:text-slate-900">
              Privacy
            </a>
            <a href="mailto:info@cogmyra.com" className="hover:text-slate-900">
              Contact
            </a>
          </div>
        </div>
      </footer>
    </div>
  );
}

function App() {
  // Simple path-based “router”
  const pathname =
    typeof window !== "undefined" ? window.location.pathname : "/";

  if (pathname === "/guide") {
    return <GuidePage />;
  }

  return <LandingPage />;
}

export default App;
